A <- matrix(1:100, nrow=10)  
B <- matrix(1:1000, nrow=10)

# Transpose A and B  
t(A)
t(B)

# Create vectors
a <- c(1:100)
b <- c(1:1000)

X <- A*a
Y <- B*b

a=c(1:100)
b=c(1:1000)

A*a %*%
B*b %*%
  
S=matrix(A, nrow=2)
det(S)

det(A)
det(B)
det(X)
det(Y)